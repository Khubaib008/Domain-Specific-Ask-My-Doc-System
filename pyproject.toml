[project]
name = "rag-system"
version = "0.1.0"
description = "Production-grade domain-specific RAG system"
requires-python = ">=3.12"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
